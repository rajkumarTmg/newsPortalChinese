import { SET_SUBSCRIPTION_SUCCESS_VERIFIED_POPUP } from '../types/types';

const setSubscriptionSuccessVerifiedPopup = payload => ({
    type: SET_SUBSCRIPTION_SUCCESS_VERIFIED_POPUP,
    payload
});

export default setSubscriptionSuccessVerifiedPopup;

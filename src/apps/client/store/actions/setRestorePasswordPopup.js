import { SET_RESTORE_PASSWORD_POPUP } from '../types/types';

const setRestorePasswordPopup = payload => ({
    type: SET_RESTORE_PASSWORD_POPUP,
    payload
});

export default setRestorePasswordPopup;

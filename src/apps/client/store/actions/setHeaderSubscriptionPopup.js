import { SET_HEADER_SUBSCRIPTION_POPUP } from '../types/types';

const setHeaderSubscriptionPopup = payload => ({
    type: SET_HEADER_SUBSCRIPTION_POPUP,
    payload
});

export default setHeaderSubscriptionPopup;
